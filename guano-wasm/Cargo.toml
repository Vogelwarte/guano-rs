[package]
name = "guano-wasm"
version = "0.0.1"
edition = "2024"
license = "MIT"
authors = ["vogelwarte.ch", "Mario Fischer"]

[dependencies]
# default-features = false drops the CLI's clap/serde_json tree from the wasm build.
guano-rs = { path = "../guano-rs", default-features = false }
wasm-bindgen = "0.2.105"
wasm-bindgen-futures = "0.4.56"
web-sys = { version = "0.3.82", features = ["Exception", "File", "console"] }

[profile.release]
lto = true

[lib]
crate-type = ["cdylib"]

[dev-dependencies]
wasm-bindgen-test = "0.3.55"
